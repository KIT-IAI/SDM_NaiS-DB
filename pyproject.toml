[project]
name = "nais-datenbank"
version = "0.2.0"
description = "NaiS database build pipeline"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "openpyxl",
    "pandas",
]

[tool.uv]
package = false
